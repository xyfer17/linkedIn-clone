import React from 'react'
import styled from "styled-components";
import Header from './Header';

const Home = (props) => {
  return (
    <div>
        <Header/>
    </div>
  )
}

export default Home
